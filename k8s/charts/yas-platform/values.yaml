# Shared defaults. Environment-specific image tags and NodePorts live in
# values-dev.yaml and values-staging.yaml.
global:
  domain: yas.local
  imageTag: latest
  environment: dev

nodePorts:
  enabled: false
  storefront: 30080
  backoffice: 30081
  api: 30082

yas-configuration:
  enabled: true
